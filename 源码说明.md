# WOLF RPG 地图导出工具 v1.0 — 源码说明

本工具为完全离线的 WOLF RPG Editor 地图渲染/导出器。以下说明各源码文件的作用。

## 核心渲染模块（exe 运行依赖）

| 文件 | 作用 |
|---|---|
| `wolfrpg_map_gui.py` | GUI 主程序（tkinter）。选择游戏 Data 文件夹 → 地图列表（勾选/全选/反选）→ 预览（地图层/事件层/两者，完整尺寸+滚动+拖拽）→ 导出（当前/所选）→ 复制到剪贴板。事件显隐按地图记忆，复制/导出时隐藏事件不出现。 |
| `offline_render_quadrant.py` | 离线渲染主逻辑。读取 `.mps`，按 base/自动图块/墙角规则渲染三层，输出 PNG。含 aid 感知墙角规则表查表、邻居回退。 |
| `offline_render_base.py` | 基础图块集加载（`load_base_tiles`、`tile_rc`）、TileSetData 查找、多编码文件解析器 `resolve_asset_file`（支持日文/中文文件名）。 |
| `render_event_layer.py` | 事件层离线渲染。解析事件页（角色/图块事件），加载 CharaChip/MapChip 素材，绘制到透明 PNG。 |
| `parse_mps_events.py` | 解析 `.mps` 的事件区（事件/页/命令）。 |
| `parse_common_events.py` | 通用命令流解析（`read_tstr`、`parse_route` 等）。 |
| `wolfrpg.py` | `.mps` 0x65/0x66 格式解析（`parse_mps`），TileSetData 解析（`parse_tileset_dat`），含非方形布局修复、无图块数据/截断文件保护。 |
| `wolfrpg_067.py` | `.mps` 0x67（LZ4 压缩）格式解析（`decode_mps_0x67`、`parse_067_header`、`_logical_bytes`）。 |

## 数据文件

| 文件 | 作用 |
|---|---|
| `autotile_rule_norm.json` | aid 感知墙角规则表（key=`aid;掩码;四位数字` → 象限行）。由唯一色导出后重建得到。 |
| `map_backgrounds.json` | 地图背景图配置表（当前为空，保持通用）。 |
| `mappings/` | 各图块组的 TileSetData 解析缓存/映射表（如 autotile 映射、aid_to_file_index）。 |

## 调试 / 提取工具源码

| 文件 | 作用 |
|---|---|
| `export_spawn.py` | **基于编辑器导出 PNG**：启动 WOLF 编辑器、设置当前地图、等待渲染后截图导出为 PNG（唯一色/真实素材导出用）。 |
| `_make_unique_all.py` | 生成“唯一色素材”：把图块组替换为唯一色填充图（每个图块/象限有唯一 RGB，用于后续自动提取墙角规则）。 |
| `_make_numbered_materials_t1/t3/t9.py` | 早期分 tileset 的编号唯一色素材生成脚本（t1/t3/t9 图块组）。 |
| `_rebuild_rule_table_aid.py` | **重建墙角规则表**：从多张补全的唯一色参考图 + 地图数据，提取每个 `aid;掩码;数字` 特征的象限行，合并写入 `autotile_rule_norm.json`（覆盖时保留更具体的行）。 |
| `_export_both_layers.py` | 双图层批量导出：对全部地图分别输出“地图层”和“事件层”两个文件夹。 |
| `_batch_render_corner.py` | 批量离线渲染全部地图到指定文件夹（墙角规则版）。 |
| `_write_mps_test.py` | 0x67 格式写入/回读测试（round-trip 验证）。 |

## 使用流程（重建墙角规则表）

1. 用 `_make_unique_all.py` 把游戏的图块组替换为唯一色素材；
2. 用 `export_spawn.py` 导出目标地图的唯一色 PNG（大图可能被截断，需人工补全）；
3. 在编辑器中调整/修正地图图层后重新导出并补全唯一色参考图；
4. 运行 `_rebuild_rule_table_aid.py` 从补全参考图重建 `autotile_rule_norm.json`；
5. 还原真实素材，离线渲染验证。

版本：1.0
